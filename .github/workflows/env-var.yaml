name: environment
on: [ workflow_dispatch ]
jobs: 
  env-var-and-context:
    runs-on: ubuntu-latest
    steps: 
        - name: Log environment
          run: echo $GITHUB_REF
        - name: Log context
          run: echo '${{ github.ref}}'